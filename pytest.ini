[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --screenshot=only-on-failure --tracing=retain-on-failure --video=retain-on-failure
